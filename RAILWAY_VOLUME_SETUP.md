# Як Налаштувати Railway Volume для Збереження Налаштувань

## Крок 1: Відкрийте Ваш Проект на Railway

1. Перейдіть на [railway.app](https://railway.app)
2. Увійдіть до свого акаунту
3. Виберіть проект з ботом субтитрів

## Крок 2: Додайте Volume

### Через Web Interface:

1. **Натисніть на ваш сервіс** (бот) у проекті
2. Перейдіть до вкладки **"Settings"** (Налаштування)
3. Прокрутіть вниз до розділу **"Volumes"**
4. Натисніть кнопку **"+ New Volume"** або **"Add Volume"**

### Налаштування Volume:

```
Mount Path: /app/data
```

**Важливо**: Mount path має бути точно `/app/data`

## Крок 3: Змініть Код (Вже Зроблено!)

Потрібно змінити `SETTINGS_FILE` у файлі `bot.py`:

```python
# Змініть цей рядок (біля лінії 16):
SETTINGS_FILE = "user_settings.json"

# На:
SETTINGS_FILE = "/app/data/user_settings.json"
```

## Крок 4: Створіть Директорію (Автоматично)

Додайте цей код після визначення `SETTINGS_FILE`:

```python
# Після визначення SETTINGS_FILE
import os

# Створити директорію, якщо не існує
os.makedirs(os.path.dirname(SETTINGS_FILE), exist_ok=True)
```

## Крок 5: Deployment

1. Збережіть зміни у `bot.py`
2. Закомітьте та запуште:
   ```bash
   git add bot.py
   git commit -m "Налаштування Volume для збереження налаштувань"
   git push origin main
   ```
3. Railway автоматично задеплоїть

## Крок 6: Перевірка

Після деплою перевірте логи Railway:

```
✅ Шукайте: "Created directory /app/data" або подібні повідомлення
✅ Налаштування користувачів тепер зберігаються навіть після рестартів
```

## Альтернативний Метод: Railway CLI

Якщо ви використовуєте Railway CLI:

```bash
# Додати volume
railway volume add

# Вкажіть mount path
Mount path: /app/data
```

## Важливо! ⚠️

### Що НЕ зберігається у Volumes:
- Тимчасові відео файли (це нормально, вони одразу видаляються)
- Логи (зберігаються іншим способом)

### Що ЗБЕРІГАЄТЬСЯ:
- ✅ `user_settings.json` - налаштування користувачів
- ✅ Будь-які інші файли у `/app/data`

## Troubleshooting

### Помилка "Permission denied":
```python
# Додайте перевірку прав
try:
    os.makedirs("/app/data", exist_ok=True)
    # Перевірити можливість запису
    test_file = "/app/data/.test"
    with open(test_file, 'w') as f:
        f.write("test")
    os.remove(test_file)
    print("✅ Volume доступний для запису")
except Exception as e:
    print(f"❌ Помилка volume: {e}")
```

### Volume не з'являється:
1. Перевірте, чи Volume створений у налаштуваннях Railway
2. Перевірте правильність mount path
3. Перезапустіть сервіс

## Візуальний Приклад

```
Railway Project Dashboard
├── 🤖 subtitle-bot (ваш сервіс)
│   ├── ⚙️ Settings
│   │   ├── 🔐 Environment Variables
│   │   │   ├── TELEGRAM_BOT_TOKEN
│   │   │   └── GROQ_API_KEY
│   │   └── 💾 Volumes
│   │       └── ➕ New Volume
│   │           ├── Mount Path: /app/data
│   │           └── ✅ Create
```

## Перевірка Роботи

Після налаштування:

1. Надішліть відео боту
2. Змініть налаштування (шрифт, колір, розмір)
3. Обробіть відео
4. **Перезапустіть Railway сервіс** (Settings → Deploy → Restart)
5. Надішліть нове відео
6. ✅ Налаштування мають залишитися такими ж!

---

**Примітка**: Я не можу безпосередньо налаштувати Volume на вашому Railway акаунті, оскільки не маю доступу до вашого проекту. Але ви можете легко зробити це самостійно, слідуючи цьому гіду! 😊
