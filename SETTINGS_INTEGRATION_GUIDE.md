# 🔧 Інтеграція Пам'яті Налаштувань Шрифтів на Railway

## ✅ Ваш код вже підтримує збереження налаштувань!

Я перевірив ваш код - **механізм збереження вже інтегровано**:
- ✅ Налаштування зберігаються автоматично при кожній зміні
- ✅ Налаштування завантажуються при обробці нового відео
- ✅ Підтримка Railway Volume

## 🏗️ Налаштування Railway Volume (Обов'язково!)

### Крок 1: Увійдіть в Railway Dashboard

1. Відкрийте https://railway.app
2. Увійдіть в акаунт
3. Виберіть ваш проект з ботом субтитрів

### Крок 2: Створіть Volume

1. Натисніть на **ваш сервіс** (бот) в проекті
2. Виберіть вкладку **Settings** (⚙️)
3. Прокрутіть до розділу **Volumes** (💾)
4. Натисніть **"+ New Volume"**
5. Вкажіть **Mount Path**: `/app/data`
6. Натисніть **Create**

### Крок 3: Перезапустіть Сервіс

1. У вкладці **Deployments**
2. Натисніть **"Restart"** або чекайте автоматичного деплою

### Крок 4: Перевірте Логи

Після перезапуску перевірте логи:

```
✅ Шукайте: "✅ Settings directory ready: /app/data"
❌ Якщо бачите: "⚠️ Settings directory issue" - Volume не підключений
```

## 🧪 Перевірка Роботи

### Локально (Windows):

```powershell
# Запустіть тестовий скрипт
python test_settings.py
```

### На Railway:

1. Надішліть відео боту
2. Змініть налаштування (шрифт, розмір, колір)
3. Обробіть відео
4. **Перезапустіть сервіс** (Settings → Restart)
5. Надішліть нове відео
6. ✅ **Перевірте**: налаштування мають бути такими ж!

## 📊 Що Зберігається

Ваш бот зберігає для кожного користувача:

| Налаштування | Ключ | Приклад |
|--------------|------|---------|
| Шрифт | `font_name` | "Peace Sans" |
| Розмір шрифту | `fontsize` | 93 |
| Колір тексту | `color_name`, `color_value` | "Білий", "&H00FFFFFF" |
| Відступ знизу | `margin_bottom` | 30 |
| Тінь | `shadow_enabled` | true |
| Обводка | `outline_enabled` | true |
| Слів у рядку | `wpl` | 2 |
| Рядків на сторінці | `max_lines` | 1 |
| Анімація | `animation` | false |
| Караоке | `karaoke` | false |
| Колір підсвітки | `highlight_color_name`, `highlight_color_value` | "Жовтий", "&H0000FFFF" |

## 🐛 Troubleshooting

### Проблема: Налаштування не зберігаються між сесіями

**Причина**: Volume не налаштований на Railway

**Рішення**: Виконайте кроки 1-3 вище

### Проблема: Помилка прав доступу

**Перевірте логи**:
```
⚠️ Settings directory issue: [Permission denied]
```

**Рішення**: 
1. Перевірте, чи Volume створений
2. Перевірте Mount Path: `/app/data`
3. Перезапустіть сервіс

### Проблема: Файл не створюється

**Перевірте локально**:
```powershell
python test_settings.py
```

**На Railway**:
- Перевірте логи Railway
- Переконайтеся, що Volume під'єднаний
- Перевірте, що Mount Path = `/app/data`

## 📝 Додаткова Інформація

### Де зберігаються налаштування?

**На Railway з Volume**:
```
/app/data/user_settings.json
```

**Локально (Windows)**:
```
.\user_settings.json
```

### Структура файлу `user_settings.json`:

```json
{
  "123456789": {
    "fontsize": 93,
    "color_name": "Жовтий",
    "color_value": "&H0000FFFF",
    "font_name": "Peace Sans",
    "margin_bottom": 30,
    "shadow_enabled": true,
    "outline_enabled": true,
    "wpl": 2,
    "max_lines": 1,
    "animation": false,
    "karaoke": false,
    "highlight_color_name": "Червоний",
    "highlight_color_value": "&H000000FF"
  },
  "987654321": {
    ...
  }
}
```

Де ключі (`"123456789"`, `"987654321"`) - це Telegram Chat ID користувачів.

## ✅ Висновок

**Ваш код повністю готовий!** 

Єдине, що потрібно зробити:
1. ✅ Створити Volume на Railway (Mount Path: `/app/data`)
2. ✅ Перезапустити сервіс
3. ✅ Перевірити логи

Після цього всі налаштування будуть зберігатися автоматично для кожного користувача! 🎉

---

**Створено**: ${new Date().toISOString()}
